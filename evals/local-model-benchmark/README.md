# Local Model Benchmark

Run this benchmark when comparing local models or prompt variants.

```bash
/home/dima/AI-Harness/scripts/run-local-model-eval qwen2.5-coder:1.5b
```

Outputs are written to `/home/dima/models/evals/<timestamp>-<model>/`.

Score outputs with `AI-Harness/rubrics/model-eval-rubric.md`.

